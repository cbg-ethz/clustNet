---
title: "netClust"
output: rmarkdown::html_vignette
description: >
  Network-based clustering of binary data. Optional adjustment for covariates. 
vignette: >
  %\VignetteIndexEntry{netClust}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(netClust)
```


### Demonstration:
Simulate binary data and learn clusters subsequently.

```{r, eval = FALSE}
# Simulate binary data
ss <- c(400,500,600) # samples in each cluster
my_data <- sampleData(kclust = 3, Nvars = 20, sseed = 2, samplesizes = ss)

# Network-based clustering
cluster_res_t <- netClust(myData, kclust = 3)
cluster_res <- netCluster(myData, kclust = 3)
cluster_res_test <- netClustParallel(myData, kclust = 3)
cluster_res_p <- netClusterParallel(myData, kclust = 3)
```
